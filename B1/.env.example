# B1 — copy to .env. .env is gitignored; .env.example is committed as docs.

# PostgreSQL connection string. Matches the bundled docker-compose service.
DATABASE_URL="postgresql://sabi:sabi@localhost:5432/sabi_documents?schema=public"

# Port the Express server listens on (optional, defaults to 3001)
PORT=3001
