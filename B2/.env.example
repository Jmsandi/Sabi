# B2 — copy to .env (optional), then run `node src/index.js "your query"`.
# .env is gitignored; .env.example is committed as documentation.

# Contact email to join the OpenAlex "polite pool" (faster, more predictable
# rate limits). Optional — the module works without it.
OPENALEX_MAILTO=you@example.org
